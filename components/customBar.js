import { View, Text, StatusBar } from 'react-native'
import React from 'react'

const CustomBar = () => {
  return (
   <StatusBar animated={true} backgroundColor={'#f00f'} />
  )
}

export default CustomBar